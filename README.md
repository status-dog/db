# db


Only a very basic setup for now

## Start and migrate

``` shell
./start.sh
```


## Docker container
``` shell
docker-compose up
```

## DB migrations

settings via ENVIRONMENT or `.env` file.

``` shell
dbmate migrate
```
